const { OK } = require('@utils/helper');
const { logger } = require('@utils/logger');

/**
 * <%= name %>
 * @public
 */
exports.<%= name %> = async (event) => {
  logger.debug(event);
  return OK();
};
