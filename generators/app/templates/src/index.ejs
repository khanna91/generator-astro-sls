const awsXRay = require('aws-xray-sdk');
const awsSdk = awsXRay.captureAWS(require('aws-sdk')); // eslint-disable-line

// define all the routes/handlers
const helloRoute = require('./api/v1/hello');

module.exports.hello = helloRoute;
