const middy = require('middy');
const { routeValidator } = require('../../../middlewares/route-validator');
const controller = require('./<%= name %>.controller');
const validator = require('./<%= name %>.validator');

/**
 * @api {<%= method %>} <%= apibase %>/<%= apiversion %>/<%= name %> <%= name %>
 * @apiDescription <%= apidesc %>
 * @apiVersion 1.0.0
 * @apiName <%= name %>
 * @apiPermission public
 */
const handler = middy(controller.<%= name %>).use(routeValidator({ schema: validator.joiSchema }));

module.exports = handler;
